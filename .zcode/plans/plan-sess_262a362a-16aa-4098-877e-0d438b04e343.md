## Plan: 3 English categories — daily-paper / tech / musings

### Architecture recap
- `category` = subdirectory name under `content/` (derived in `posts.ts:31`, not frontmatter). Flat files → `undefined` → currently all 10 show as「未分类」.
- `page.tsx:12-18` already builds the filter pill list dynamically from actual post counts and **auto-hides zero-count categories**. No code change needed there.
- Only **two** files hardcode category keys: `src/lib/categories.ts` and `src/components/writing/coverArtSvg.ts`. Everything else (WritingIndex, PostCard, CardTextLayer, article page, RSS, types.ts) is key-agnostic.

### Changes

**1. `src/lib/categories.ts`** — replace the 4 keys with the 3 new ones:
```ts
export type CategoryKey = "daily-paper" | "tech" | "musings";
export const CATEGORIES = [
  { key: "daily-paper", label: "Daily Paper" },
  { key: "tech",        label: "Engineering" },
  { key: "musings",     label: "Musings" },
];
```
(`categoryLabel`/`未分类` fallback unchanged.)

**2. `src/components/writing/coverArtSvg.ts:176-217`** — remap the motif switch:
- `daily-paper` → keep the book-spine/citation motif (was `paper-reading`).
- `tech` → keep the stele-rectangle array (unchanged).
- `musings` → reuse the breathing dot-matrix motif (was `soul`).
- Drop the `projects` branch. Update the comment block at 176–180.

**3. Content restructure** — create 3 subdirs and `git mv` the 10 files:

`content/daily-paper/` (6)
- a-paper-called-RSI.md
- gpt-red-self-play-red-team.md
- llm-as-a-coach.md
- llms-cant-jump-world-model.md
- open-reasoning-cot.md
- credit-assignment-is-all-you-need.md

`content/tech/` (4)
- agentenv-kimi-agent-infra.md
- context-compaction-three-bets.md
- agent-is-not-a-smarter-search.md
- how-to-build-good-skill.md

`content/musings/` — created but empty (0 posts → auto-hidden).

**4. Cleanup** — `content/agent-is-not-a-smarter-search.md:5` has a dead `category: "AI"` frontmatter field (loader ignores it, uses dir). Leave as-is (harmless) or strip — I'll strip it for cleanliness since we're touching the file's location anyway. → Actually leaving frontmatter untouched is lower-risk; the loader ignores it regardless. I'll leave it.

### Result
Filter pills: `All (10) · Daily Paper (6) · Engineering (4)`. `Musings` hidden until a post lands in `content/musings/`. No type, UI, RSS, or routing impact.

### Verification
- `npm run build` (or lint) to confirm no broken references.
- Spot-check `/writing` renders with 3 visible pills, 10 cards total, motifs render per category.